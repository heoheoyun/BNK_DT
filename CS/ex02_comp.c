#include <stdio.h>

int main(){
	/*
	보수 (보충해주는 수) 알아가기
	
	//이진법에서 1의 보수는 (3개의 비트로-7~7 까지 표현가능)
	//양의 정수를 나타내는 2진수 맨앞에 비트1개 추가 후 각 비트들을 반전
	6 -> 110 -> 0110 -> 1001
	2의 보수는 1의 보수값에 +1  (3개의 비트로 -8~7 까지 표현가능) 
	6 -> 110 -> 0110 -> 1001 -> 1010
	
	2의 보수에서 1000은 -8 이다.
	 
	*/
	return 0;
}

